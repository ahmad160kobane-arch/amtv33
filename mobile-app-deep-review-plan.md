# 🔍 مراجعة عميقة لتطبيق الهاتف - خطة الإصلاح

## المشاكل الحرجة المكتشفة

### 🔴 مشاكل التوافق مع الباك اند والسيرفر السحابي

1. **القنوات المجانية تتطلب بريميوم على السيرفر السحابي**
   - `POST /api/stream/live/:channelId` يستخدم `requireAuth + requirePremium`
   - التطبيق يرسل طلبات القنوات المجانية لنفس النقطة
   - **النتيجة**: المستخدمين المجانيين لا يمكنهم مشاهدة القنوات الحرة

2. **`fetchFreeChannels` يرسل `group` لكن السيرفر يتوقع `category`**
   - الموبايل: `q.set("group", params.group)`
   - السيرفر السحابي يقرأ `category` param
   - **النتيجة**: فلتر التصنيفات لا يعمل

3. **نقص حقول في استجابة login/register**
   - `balance`, `stats`, `role` غير موجودة في response
   - **النتيجة**: واجهة الحساب تعرض بيانات ناقصة

4. **عدم إرسال `deviceId` في طلبات البث**
   - السيرفر السحابي يعتمد على `deviceId` لإدارة الجلسات
   - الموبايل لا يرسله
   - **النتيجة**: مشاكل في إدارة عدد الاتصالات

5. **`fetchPremiumChannels` لا يرسل JWT**
   - يستخدم `fetch` عادي بدون توكن
   - **النتيجة**: القنوات المميزة قد لا تعمل

### 🟠 مشاكل UI/UX

6. **عدم تناسق الهيدر بين التبويبات**
   - الرئيسية ومباشر: هيدر متحرك (sticky)
   - ترفيه وأطفال: هيدر ثابت
   - قائمتي: بدون هيدر متحرك

7. **محاذاة RTL خاطئة في HeroSlider**
   - `alignItems: 'flex-start'` يجب أن تكون 'flex-end' في RTL
   - زر "شاهد الآن" محاذاة خاطئة

8. **حجم النصوص صغير جداً للعربية**
   - عناوين البطاقات: fontSize 10-12 صغير جداً
   - أسماء القنوات: fontSize 12 غير كافٍ

9. **البحث يتطلب ضغط Enter**
   - لا يوجد بحث تلقائي مع تأخير (debounce)
   - تجربة مستخدم سيئة

10. **قفل البريميوم يظهر فوق العناصر القابلة للنقر**
    - المستخدم ينقر على عنصر مقفل ويتم التنقل ثم تظهر رسالة

11. **عدم وجود حالة التحميل لصور الشعارات**

12. **الـ WebView ad blocker شديد جداً**
    - يحظر مجالات مشغلة فعلية
    - يسبب عدم عمل بعض المصادر

## خطة الإصلاح بالترتيب

### المرحلة 1: إصلاح التوافق مع الباك اند (حرج)
- [x] إصلاح `fetchFreeChannels` لإرسال `category` بدل `group`
- [x] إصلاح `requestFreeStream` لدعم القنوات المجانية بدون بريميوم
- [x] إضافة `deviceId` لطلبات البث
- [x] إصلاح `fetchPremiumChannels` لإرسال JWT

### المرحلة 2: إصلاح UI/UX
- [x] إصلاح محاذاة RTL في HeroSlider
- [x] تحسين حجم النصوص العربية
- [x] إضافة بحث تلقائي (debounce)
- [x] تحسين عرض القفل البريميوم
- [x] تناسق الهيدر بين التبويبات
